packages:
  - wireguard

users:
  - name: test
    groups: [adm]
    shell: /bin/bash
    # TODO: perform all base config that needs root access in cloud-init itself.
    sudo: "ALL=(ALL) NOPASSWD:ALL"
    ssh_authorized_keys:
      - ${ssh_key}
